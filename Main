import numpy as np

# Datos de entrada
X = np.array([[1, 2]])

# Pesos
W = np.array([[0.5, -1],
              [1.5, 2]])

# Resultado
resultado = np.dot(X, W)

print(resultado)

import numpy as np

# Datos de entrada
X = np.array([[1, 2]])

# Pesos
W = np.array([[0.5, -1],
              [1.5, 2]])

# Resultado
resultado = np.dot(X, W)

print(resultado)
